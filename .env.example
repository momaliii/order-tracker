# Database
# Option 1: Supabase (Recommended - Easy setup)
# Get your connection string from: Supabase Dashboard → Project Settings → Database → Connection String
# Use "Connection pooling" for the app, and "Direct connection" for Prisma migrations.
#
# Example (app runtime / pooling):
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require"
#
# Example (migrations / direct):
# DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require"
#
# For local Postgres, you can set DIRECT_URL=DATABASE_URL.
DATABASE_URL="postgresql://user:password@localhost:5432/attribution_tracker?schema=public"
DIRECT_URL="postgresql://user:password@localhost:5432/attribution_tracker?schema=public"

# Option 2: Local PostgreSQL
# DATABASE_URL="postgresql://user:password@localhost:5432/attribution_tracker?schema=public"

# Option 3: Other PostgreSQL hosting (Railway, Render, etc.)
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Server
PORT=3000
NODE_ENV=development

# EasyOrders Webhook Secret (set this when you create the webhook in EasyOrders dashboard)
EASYORDERS_WEBHOOK_SECRET=""

# Supabase Auth (Option A)
# Used by the backend to verify dashboard JWTs via JWKS.
SUPABASE_URL=""
# Comma-separated list of admin emails allowed to change settings from dashboard.
ADMIN_EMAILS="you@domain.com"

# API Keys (optional, for future platform integrations)
META_ACCESS_TOKEN=""
TIKTOK_ACCESS_TOKEN=""
GOOGLE_CLIENT_ID=""
SNAP_ACCESS_TOKEN=""

# CORS (comma-separated list of allowed origins)
ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000"
